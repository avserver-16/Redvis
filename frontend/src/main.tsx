
import React from 'react';
import ReactDOM from 'react-dom/client';
import CacheVisualizer from './CacheVisulaizer/CacheVisualizer';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CacheVisualizer />
  </React.StrictMode>,
);
